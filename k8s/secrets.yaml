# Template - DO NOT commit with real values
# Use: kubectl create secret generic coderabbit-secrets --from-env-file=.env.production
apiVersion: v1
kind: Secret
metadata:
  name: coderabbit-secrets
  namespace: coderabbit
type: Opaque
stringData:
  database-url: "postgresql://coderabbit:PASSWORD@postgres:5432/coderabbit"
  redis-url: "redis://:PASSWORD@redis:6379"
  jwt-secret: "REPLACE_WITH_32_CHAR_SECRET"
  github-token: "ghp_REPLACE"
  github-webhook-secret: "REPLACE"
  openai-api-key: "sk-REPLACE"
  anthropic-api-key: "sk-ant-REPLACE"
  cohere-api-key: "REPLACE"
